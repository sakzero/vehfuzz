campaign:
  name: bluetooth-sdp-pcap-offline-demo
  protocol: bluetooth
  protocol_config:
    layer: sdp
    preserve_sdp_param_len: true
  mode: offline
  cases: 200
  rng_seed: 9
  seed:
    type: pcap
    path: ../../../samples/sample_bt_sdp.pcap
  mutators:
    - type: havoc
      max_ops: 4

